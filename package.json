{
  "name": "project-updates-bot",
  "version": "1.0.0",
  "description": "Slack bot for project updates with Notion-like UI",
  "main": "server.py",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.ts"]
  }
}
